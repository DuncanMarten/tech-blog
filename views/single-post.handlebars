{{> post-info post }}

{{#if loggedIn}}
<form class="comment-form">
  <div class="card">
    <div class="card-header"></div>
    <div class="card-body">
      <label for="comment-body" class="forn-label">Comment</label>
      <textarea id="comment-body" class="form-control" name="comment-body"></textarea>
      <div class="d-grid">
        <button class="btn btn-primary" type="submit">Submit</button>
      </div>
    </div>

  </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}